// Cross-platform keybindings.
// On macOS swap "ctrl" for "cmd" where you like — these are tuned for Windows + Linux.
[
  // ─── Window / file navigation ────────────────────────────────
  {
    "key": "ctrl+shift+e",
    "command": "workbench.view.explorer"
  },
  {
    "key": "ctrl+shift+f",
    "command": "workbench.action.findInFiles"
  },
  {
    "key": "ctrl+shift+g",
    "command": "workbench.view.scm"
  },
  {
    "key": "ctrl+`",
    "command": "workbench.action.terminal.toggleTerminal"
  },
  {
    "key": "ctrl+shift+`",
    "command": "workbench.action.terminal.new"
  },

  // ─── Editor groups (splits) ──────────────────────────────────
  { "key": "ctrl+k v", "command": "markdown.showPreviewToSide" },
  { "key": "ctrl+\\",  "command": "workbench.action.splitEditor" },
  { "key": "ctrl+k ctrl+\\", "command": "workbench.action.splitEditorOrthogonal" },
  { "key": "ctrl+k w", "command": "workbench.action.closeEditorsInGroup" },

  // ─── Tab navigation ──────────────────────────────────────────
  { "key": "ctrl+alt+left",  "command": "workbench.action.previousEditor" },
  { "key": "ctrl+alt+right", "command": "workbench.action.nextEditor" },

  // ─── Selection / multi-cursor power ──────────────────────────
  {
    "key": "ctrl+d",
    "command": "editor.action.addSelectionToNextFindMatch",
    "when": "editorFocus && (!vim.active || vim.mode == 'Insert')"
  },
  {
    "key": "ctrl+shift+l",
    "command": "editor.action.selectHighlights",
    "when": "editorFocus && (!vim.active || vim.mode == 'Insert')"
  },
  {
    "key": "alt+up",
    "command": "editor.action.moveLinesUpAction",
    "when": "editorTextFocus && !editorReadonly && (!vim.active || vim.mode == 'Insert')"
  },
  {
    "key": "alt+down",
    "command": "editor.action.moveLinesDownAction",
    "when": "editorTextFocus && !editorReadonly && (!vim.active || vim.mode == 'Insert')"
  },
  {
    "key": "shift+alt+down",
    "command": "editor.action.copyLinesDownAction",
    "when": "editorTextFocus && !editorReadonly && (!vim.active || vim.mode == 'Insert')"
  },
  {
    "key": "shift+alt+up",
    "command": "editor.action.copyLinesUpAction",
    "when": "editorTextFocus && !editorReadonly && (!vim.active || vim.mode == 'Insert')"
  },

  // ─── Refactor / quick fix ────────────────────────────────────
  {
    "key": "ctrl+.",
    "command": "editor.action.quickFix",
    "when": "editorHasCodeActionsProvider && editorTextFocus && !editorReadonly && (!vim.active || vim.mode == 'Insert')"
  },
  {
    "key": "f2",
    "command": "editor.action.rename",
    "when": "editorHasRenameProvider && editorTextFocus && !editorReadonly && (!vim.active || vim.mode == 'Insert')"
  },

  // ─── Format ──────────────────────────────────────────────────
  {
    "key": "shift+alt+f",
    "command": "editor.action.formatDocument",
    "when": "editorHasDocumentFormattingProvider && editorTextFocus && !editorReadonly && (!vim.active || vim.mode == 'Insert')"
  },

  // ─── Testing (vitest.explorer wires into VS Code's Test API) ─
  { "key": "ctrl+; a", "command": "testing.runAll" },
  { "key": "ctrl+; r", "command": "testing.runAtCursor", "when": "!vim.active || vim.mode == 'Insert'" },
  { "key": "ctrl+; d", "command": "testing.debugAtCursor", "when": "!vim.active || vim.mode == 'Insert'" },
  { "key": "ctrl+; t", "command": "workbench.view.testing.focus" },

  // ─── Debugging (F5 start/continue is default; these add the rest) ─
  { "key": "ctrl+; c", "command": "workbench.action.debug.run" },
  { "key": "ctrl+; s", "command": "workbench.action.debug.stop", "when": "inDebugMode" },
  { "key": "ctrl+; b", "command": "editor.debug.action.toggleBreakpoint", "when": "editorTextFocus && (!vim.active || vim.mode == 'Insert')" },
  { "key": "ctrl+; l", "command": "workbench.debug.action.focusVariablesView" },

  // ─── Toggle Vim quickly while learning ───────────────────────
  {
    "key": "ctrl+shift+alt+v",
    "command": "vim.toggleVim"
  },

  // ─── Disable Tab-accept of inline suggestions (belt + braces) ─
  {
    "key": "tab",
    "command": "-editor.action.inlineSuggest.commit",
    "when": "inlineSuggestionVisible && !editorReadonly"
  }
]
